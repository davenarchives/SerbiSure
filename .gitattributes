# Auto detect text files and perform LF normalization
* text=auto

# Ensure shell scripts always use LF (Linux line endings)
*.sh text eol=lf
gradlew text eol=lf
android/gradlew text eol=lf

# Windows batch / powershell scripts
*.bat text eol=crlf
*.cmd text eol=crlf
*.ps1 text eol=crlf
